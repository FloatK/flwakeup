targets:
  $default:
    builders:
      drift_dev:
        options:
          data_class_to_companions: false
          store_date_time_values_as_text: true
      freezed:
        options:
          format: true
